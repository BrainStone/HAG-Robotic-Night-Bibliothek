package core.sensor.interfaces;

/**
 * Dieses Interface existiert aus Kompatibilitätsgründen, damit man nicht jede
 * Klasse im {@link core.sensor.SensorHandler} übergeben kann.
 */
public interface ISensorHandler {
}
